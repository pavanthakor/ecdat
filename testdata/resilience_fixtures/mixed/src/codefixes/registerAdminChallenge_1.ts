/* Deliberately unparseable, shaped like OWASP Juice Shop's
   data/static/codefixes/*.ts challenge snippets: a fragment cut out of a
   larger file. One of these aborted the whole source scan before ADR-0033. */
      }
    })
  app.post('/api/Users', verify.registerAdminChallenge(), (req: Request, res: Response, next: NextFunction) => {
    const role = req.body.role ||
  }}}
  export default {{ role: 'admin' ::
