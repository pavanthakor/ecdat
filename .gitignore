.venv/
__pycache__/
*.pyc
node_modules/
*.db
.ecdat-scratch/
reports-out/
# API key files (ADR-0035). They hold digests, not tokens -- and still never
# belong in a commit. The default location is outside the repo anyway.
api-keys*.json
